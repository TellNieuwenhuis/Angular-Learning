let log = function (msg) {
    console.log(msg);
}

let doLog = (msg) => { //* shorter way of doing it with an arrow funcion.
    console.log(msg);
}

//* shorter and cleaner way of doing it if you only have one line of code.
//! let doLog = (msg) => console.log(msg); 